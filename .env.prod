BASE_URL=https://client.slideruleearth.io

# GitHub OAuth Configuration (for organization membership verification)
# Production uses a separate GitHub OAuth App from test
# Fill these in after deploying: make deploy-github-oauth-prod
VITE_GITHUB_CLIENT_ID=
VITE_GITHUB_OAUTH_API_URL=
